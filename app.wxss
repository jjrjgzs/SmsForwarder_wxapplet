.container {
    width: 100%;
    height: 100%;
    display: flex;
    flex-direction: column;
    /* iOS底部有小黑条，窗口底部要设置一个papdding安全区，防止内容被小黑条挡住*/
    padding-bottom: calc(20rpx + constant(safe-area-inset-bottom)); /* 兼容 iOS < 11.2 */
    padding-bottom: calc(20rpx + env(safe-area-inset-bottom)); /* 兼容 iOS >= 11.2 */
}